<mujoco model="scene">
    <include file="so101_new_calib.xml" />

    <visual>
        <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0" />
        <rgba haze="0.15 0.25 0.35 1" />
        <global azimuth="160" elevation="-20" />
    </visual>

    <asset>
        <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512"
            height="3072" />
        <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4"
            rgb2="0.1 0.2 0.3"
            markrgb="0.8 0.8 0.8" width="300" height="300" />
        <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5"
            reflectance="0.2" />
    </asset>

    <worldbody>
        <light pos="0 0 3.5" dir="0 0 -1" directional="true" />
        <geom name="floor" size="0 0 0.05" pos="0 0 0" type="plane" material="groundplane" />

        <!-- 📷 오버헤드 카메라 -->
        <camera name="overhead_cam" pos="0.35 0.0 0.45" mode="targetbody" target="target_marker" fovy="50" />
        <!-- 📷 측면 사선 카메라 -->
        <camera name="side_cam" pos="0.4 -0.35 0.3" mode="targetbody" target="target_marker" fovy="45" />

        <!-- 🎯 1. 목표 타겟 마커 (mocap_pos[0] - Glowing Green Sphere) -->
        <body name="target_marker" pos="0.25 0.0 0.1" mocap="true">
            <geom name="target_geom" type="sphere" size="0.02" rgba="0.1 0.9 0.3 0.75" contype="0" conaffinity="0" />
        </body>

        <!-- 📍 2. 현재 관절각(current_qpos) 기반 실시간 FK 마커 (mocap_pos[1] - Glowing Cyan Sphere) -->
        <body name="curr_ee_fk_marker" pos="0.25 0.0 0.1" mocap="true">
            <geom name="curr_ee_fk_geom" type="sphere" size="0.015" rgba="0.0 0.8 1.0 0.85" contype="0" conaffinity="0" />
        </body>

        <!-- 🟣 3. AI 예측 목표각(predicted_action) 기반 목적지 FK 마커 (mocap_pos[2] - Glowing Magenta Sphere) -->
        <body name="pred_ee_fk_marker" pos="0.25 0.0 0.1" mocap="true">
            <geom name="pred_ee_fk_geom" type="sphere" size="0.012" rgba="0.9 0.2 0.9 0.75" contype="0" conaffinity="0" />
        </body>
    </worldbody>
</mujoco>